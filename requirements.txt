# ── Web Framework ─────────────────────────────────────────
Flask==3.1.1
Werkzeug==3.1.3          # Flask dependency (routing, security utils)

# ── Environment Variables ─────────────────────────────────
python-dotenv==1.1.0     # Loads .env into os.environ

# ── Production WSGI Server (optional, replaces Flask dev server) ──
gunicorn==23.0.0         # Use: gunicorn app:app -w 4 -b 0.0.0.0:5000